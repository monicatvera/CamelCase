{
  "name": "camelcase-kata",
  "version": "1.0.0",
  "description": "kata CamelCase",
  "scripts": {
    "compile": "tsc --noEmit",
    "compile:watch": "npm run compile -- --watch",
    "compile:build": "tsc -b",
    "test": "jest --verbose",
    "test:watch": "npm run test -- --watchAll",
    "test:coverage": "npm run test -- --coverage",
    "check-updates": "ncu -u"
  },
  "devDependencies": {
    "@types/jest": "^29.2.4",
    "jest": "^29.3.1",
    "npm-check-updates": "^16.5.1",
    "ts-jest": "^29.0.3",
    "typescript": "^4.9.5"
  }
}
